sudo: false
dist: trusty
language: cpp
compiler: gcc
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-6
      - g++-6
      - cmake
      - gdal-bin
      - libcgal-dev
      - libgdal-dev
      - libboost-dev
      - libboost-filesystem-dev
      - libboost-locale-dev
      - libyaml-cpp0.5
env: COMPILER=g++-6
# Someone should rename this folder
before_script: cd ressources
script: ./build_ubuntu1604.sh /opt

